brand slug: jakoma
LinkedIn live posting works: yes
first live post date: 2026-03-27
token env var used: JAKOMA_LINKEDIN_ACCESS_TOKEN
dry-run setting for live test: LINKEDIN_DRY_RUN=false
audit verified: yes
publish attempts verified: yes

First live single-image LinkedIn post: success
Text posting: proven live and repeatable
Single-image posting: proven live
Current blocker area remaining: Stripe verification, deployment, pilot runbook

Must persist on hosted app:
- database/socials_scheduler.db
- data/social_post_schedule.csv
- config/brands/*.json
- content/... assets and captions

---
User observations / next fixes
- Uploading assets - seems to be one upload or it overwrites, so I had to create a folder called assets and then upload all togehter.  I think individual uplaods would be better, and the option to delete
- when registering, it didn't as for my linked in details, which would be needed to post
- password reset email didn't work
- PDF upload caused a 500 and signed the user out of the flow; fixed by allowing PDFs in validation and handling upload errors gracefully.

- Added LinkedIn setup flow so brands can store profile/page URLs, author URN, token env name, and whether this is a setup-fee task.

- LinkedIn OAuth connect flow added: workspace-level token capture, callback route, disconnect, and brand readiness states.
